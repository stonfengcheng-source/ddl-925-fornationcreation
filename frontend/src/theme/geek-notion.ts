import type { ThemeConfig } from 'antd';

/** 测试和局部页面共用的 Ant Design 主题配置。 */
export const geekNotionTheme: ThemeConfig = {
  token: {
    colorPrimary: '#2563eb',
    borderRadius: 10,
    fontFamily: 'Inter, "PingFang SC", "Microsoft YaHei", sans-serif',
  },
};

export default geekNotionTheme;
