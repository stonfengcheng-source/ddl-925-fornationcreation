# Git 协作规范

团队开发需要规范的 Git 使用和协作流程，以保证代码版本可控、大幅降低协作风险和维护成本，并提高开发效率。以下是我们的 Git 协作规范。

---

## 分支命名规范

分支名应清晰地反映分支的目的，按照以下格式进行命名：

- **feature/** 新功能开发，例如：`feature/add-user-authentication`
- **fix/** Bug 修复，例如：`fix/user-login-bug`
- **hotfix/** 紧急修复生产问题，例如：`hotfix/fix-critical-crash`
- **release/** 发布分支，例如：`release/v1.0.0`
- **docs/** 文档修改，例如：`docs/update-contributing`

---

## 分支管理策略

我们采用以下分支管理策略，确保代码质量和版本管理规范化：

1. **主分支 (`main`)**
   - 主分支始终保持稳定并可部署。
   - 严禁直接提交或推送到该分支，所有改动需通过 Pull Request（PR）。

2. **开发分支 (`dev`)**
   - 功能开发以 `dev` 为基线，在该分支完成测试后再合并到 `main`。
   - 一般功能分支（`feature/*`）基于 `dev` 创建，开发完成后合并回 `dev`。

3. **功能分支和修复分支**
   - 功能和修复分支切记小而精：
     > 保持一个分支只做一件事情，推荐用多个小 PR，而非一个大 PR。

---

## Commit 信息规范

Commit 信息格式需标准化，推荐使用以下结构：

```
<type>(scope): <subject>
```

- **type**：
  - `feat`：新功能
  - `fix`：修复 Bug
  - `refactor`：重构代码（既不是新功能也不是 Bug 修复）
  - `docs`：更新文档
  - `test`：新增测试或调整测试
  - `chore`：其他杂项（如配置修改）
- **scope**：本次更改的模块或范围。
- **subject**：一句话总结本次改动。

**示例：**
- `feat(login): 新增用户登录功能`
- `fix(register): 修复注册页面表单验证问题`

---

## Pull Request（PR）规范

PR 是协作的重要环节，请严格按照以下要求提交 PR：

1. **PR 标题清晰简洁**
   - `[Feature]` 开头：如 `[Feature] 新增用户登录模块`
   - `[Fix]` 开头：如 `[Fix] 修复支付界面 crash 的问题`

2. **PR 描述包含以下要点**：
   - 修改的背景和原因。
   - 修复或实现的内容概述。
   - 有无相关 Issue 链接（比如 `Fixes #123` 表示关闭对应 Issue）。

3. **PR 尽可能小**
   - 保证每个 PR 的改动专注单一功能或问题，减少 Review 的负担。

4. **完成 PR 流程**
   - 除非紧急修复，不得自己合并 PR，必须通过至少 1 名 Reviewer 审核并明确反馈。

---

## 代码审核流程

进行代码审核（Review）时请遵循以下建议：

1. **Reviewer 的职责**
   - 确保代码功能符合需求，逻辑正确。
   - 确保代码规范遵循团队约定（如编码风格）。
   - 确保没有影响系统性能的问题。

2. **对 PR 提出改进意见**
   - 提供清晰的建议，例如代码可以如何优化。
   - 如果 PR 没有问题，通过 "Approve" 并完成合并。

---

## 其他建议

- **使用 Git 工具的基本习惯**
  - 合并代码前先拉取最新的远程分支，确保本地分支与远程分支同步。
  ```bash
  git pull origin <branch_name>
  ```
  - 及时删除已合并的本地和远程分支，保持分支管理的清洁：
  ```bash
  git branch -d <branch_name> # 删除本地分支
  git push origin --delete <branch_name> # 删除远程分支
  ```

- **使用自动化工具**：
  - 使用 GitHub Actions 设置自动化测试环境，降低手动检查的时间成本。
  - 在 PR 时引入 CI/CD 检查。

---

通过以上规范，我们可以最大限度提升团队协作的效率和代码质量！ 🎉
